{
  "version": 2,
  "builds": [
    {
      "src": "src",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    },
    {
      "src": "src/backend/tsed-app",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/backend/src/$1" },
    { "src": "/(.*)", "dest": "dist/$1" }
  ]
}
